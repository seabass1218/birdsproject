<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/header', {title: 'medicine'}); %>
  </head>
  <body>
    <% include ../partials/navbar %>

    <div class="container">
        <h1>Medicine</h1>
     
        <a href="/settings/meds/create" class="btn btn-primary">+ Add Medicine</a>
        
        <table id="medicineTable" class="table table-bordered table-striped">

          <thead>
            <tr>
              <td>Name</td>
              <td>Actions</td>
            </tr>
          </thead>
    
          <% for(var i=0; i < data.length; i++) { %>
          <tr>
            <td><%=data[i].name%></td>
            <td><a href="/settings/meds/update?id=<%= data[i]._id %>">Update</a> |
              <a href="/settings/meds/delete?id=<%= data[i]._id %>" onclick="return confirm('Are you sure you want to delete this record?')">Delete</a></td>
          </tr>
          <% } %>
        </table>
       
       </div>

    <% include ../partials/scripts %>

    <script type="text/javascript">
      $(document).ready(function () {
        $('#medicineTable').DataTable({
          responsive: true
        });
      });
    </script>
  </body>
</html>